<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Kiln.Studio.ViewModels"
        xmlns:views="using:Kiln.Studio.Views"
        xmlns:md="https://github.com/whistyun/Markdown.Avalonia"
        x:Class="Kiln.Studio.Views.ShellWindow"
        x:DataType="vm:ShellViewModel"
        Title="{Binding Title}"
        Width="1200"
        Height="800"
        Icon="/Assets/kiln-studio.ico">

    <DockPanel>

        <!-- Toolbar -->
        <Border DockPanel.Dock="Top" Classes="toolbar-host"
                IsVisible="{Binding IsProjectOpen}">
            <StackPanel Orientation="Horizontal" Spacing="4">

                <!-- Project Switcher group -->
                <Button Classes="secondary"
                        Padding="8,6">
                    <Button.Flyout>
                        <Flyout Placement="BottomEdgeAlignedLeft">
                            <StackPanel Spacing="0" MinWidth="220">
                                <!-- Recent projects -->
                                <ItemsControl ItemsSource="{Binding RecentProjects}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:RecentProjectViewModel">
                                            <Button Content="{Binding Name}"
                                                    Command="{Binding $parent[Window].((vm:ShellViewModel)DataContext).SwitchRecentCommand}"
                                                    CommandParameter="{Binding Path}"
                                                    Classes="quiet"
                                                    HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    Padding="8,6" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Separator Margin="0,4" />
                                <Button Content="Open…"
                                        Command="{Binding $parent[Window].((vm:ShellViewModel)DataContext).OpenProjectCommand}"
                                        Classes="quiet"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        Padding="8,6" />
                                <Button Content="New Site…"
                                        Command="{Binding $parent[Window].((vm:ShellViewModel)DataContext).NewSiteCommand}"
                                        Classes="quiet"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        Padding="8,6" />
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{Binding CurrentProjectName}"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="▾"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource KilnTextMutedBrush}" />
                    </StackPanel>
                </Button>

                <Button Content="Close"
                        Command="{Binding CloseProjectCommand}"
                        Classes="quiet" />

                <!-- Separator -->
                <Border Width="1"
                        Background="{StaticResource KilnBorderBrush}"
                        Margin="4,4" />

                <!-- Navigation group -->
                <Button Content="New Content"
                        Command="{Binding NewPageCommand}"
                        Classes="secondary" />
                <Button Content="Open Folder"
                    Command="{Binding OpenInFileManagerCommand}"
                    Classes="secondary" />
                <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Classes="secondary" />

                <!-- Separator -->
                <Border Width="1"
                        Background="{StaticResource KilnBorderBrush}"
                        Margin="4,4" />

                <!-- File group -->
                <Button Content="Save"
                        Command="{Binding Editor.SaveCommand}"
                        Classes="secondary" />

                <!-- Separator -->
                <Border Width="1"
                        Background="{StaticResource KilnBorderBrush}"
                        Margin="4,4" />

                <!-- Build group — primary action -->
                <Button Content="Build"
                        Command="{Binding BuildCommand}"
                        Classes="primary" />
                <CheckBox Content="Release"
                          IsChecked="{Binding ReleaseBuild}"
                          VerticalAlignment="Center"
                          Margin="4,0" />

                <!-- Separator -->
                <Border Width="1"
                        Background="{StaticResource KilnBorderBrush}"
                        Margin="4,4" />

                <!-- Preview group -->
                <Button Content="Preview"
                        Command="{Binding StartFullPreviewCommand}"
                        Classes="secondary" />
                <Button Content="Stop"
                        Command="{Binding StopFullPreviewCommand}"
                        IsEnabled="{Binding Preview.IsServing}"
                        Classes="secondary" />

                <!-- Separator -->
                <Border Width="1"
                        Background="{StaticResource KilnBorderBrush}"
                        Margin="4,4" />

                <!-- Deploy group -->
                <Button Content="Publish"
                        Command="{Binding PublishCommand}"
                        IsVisible="{Binding CanPublish}"
                        Classes="primary" />
                <Button Content="Generate deployment config"
                        Command="{Binding GenerateDeploymentConfigCommand}"
                        IsVisible="{Binding IsCiVariant}"
                        Classes="secondary" />
                <TextBlock Text="Configure deployment in Settings"
                           IsVisible="{Binding NoDeploymentVariant}"
                           VerticalAlignment="Center"
                           Classes="muted" />

                <!-- Separator -->
                <Border Width="1"
                        Background="{StaticResource KilnBorderBrush}"
                        Margin="4,4" />

                <!-- Settings -->
                <Button Content="Settings"
                        Command="{Binding OpenSettingsCommand}"
                        Classes="quiet" />

            </StackPanel>
        </Border>

        <!-- Status bar -->
        <Border DockPanel.Dock="Bottom" Classes="status-host">
            <DockPanel>
                <ProgressBar DockPanel.Dock="Right"
                             Classes="busy-indicator"
                             IsIndeterminate="True"
                             IsVisible="{Binding IsBusy}" />
                <TextBlock Text="{Binding StatusMessage}"
                           Classes="status-text"
                           TextTrimming="CharacterEllipsis" />
            </DockPanel>
        </Border>

        <Grid>
            <!-- Welcome view (no project open) -->
            <views:WelcomeView IsVisible="{Binding !IsProjectOpen}"
                               DataContext="{Binding}" />

            <!-- Three-pane content area (project open) -->
            <Grid IsVisible="{Binding IsProjectOpen}" Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" MinWidth="120" />
                    <ColumnDefinition Width="4" />
                    <ColumnDefinition Width="*" MinWidth="200" />
                    <ColumnDefinition Width="4" />
                    <ColumnDefinition Width="280" MinWidth="120" />
                </Grid.ColumnDefinitions>

                <!-- Explorer -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Search / filter / sort bar -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,4" Spacing="4">
                        <TextBox PlaceholderText="Search posts…"
                                 Text="{Binding Explorer.SearchText}"
                                 FontSize="{StaticResource FontSizeXS}" />
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <ComboBox ItemsSource="{Binding Explorer.DraftFilters}"
                                      SelectedItem="{Binding Explorer.DraftFilter}"
                                      MinWidth="90"
                                      FontSize="{StaticResource FontSizeXS}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}"
                                                   FontSize="{StaticResource FontSizeXS}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <ComboBox ItemsSource="{Binding Explorer.SortModes}"
                                      SelectedItem="{Binding Explorer.SortMode}"
                                      MinWidth="100"
                                      FontSize="{StaticResource FontSizeXS}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}"
                                                   FontSize="{StaticResource FontSizeXS}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <!-- TreeView -->
                    <TreeView Grid.Row="1"
                              x:Name="ExplorerTree"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ItemsSource="{Binding Explorer.Collections}">
                        <TreeView.DataTemplates>
                            <TreeDataTemplate DataType="vm:ContentCollectionViewModel"
                                              ItemsSource="{Binding FilteredEntries}">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold"
                                               FontSize="{StaticResource FontSizeXS}" />
                                    <TextBlock Text="("
                                               FontSize="{StaticResource FontSizeXS}"
                                               Foreground="{StaticResource KilnTextMutedBrush}" />
                                    <TextBlock Text="{Binding VisibleCount}"
                                               FontSize="{StaticResource FontSizeXS}"
                                               Foreground="{StaticResource KilnTextMutedBrush}" />
                                    <TextBlock Text=")"
                                               FontSize="{StaticResource FontSizeXS}"
                                               Foreground="{StaticResource KilnTextMutedBrush}" />
                                </StackPanel>
                            </TreeDataTemplate>
                            <DataTemplate DataType="vm:ContentEntryViewModel">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Text="{Binding Title}"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="draft"
                                               IsVisible="{Binding Draft}"
                                               Classes="draft-badge" />
                                </StackPanel>
                            </DataTemplate>
                        </TreeView.DataTemplates>
                    </TreeView>
                </Grid>

                <GridSplitter Grid.Column="1" ResizeDirection="Columns" />

                <!-- Editor (middle) -->
                <views:EditorView Grid.Column="2" DataContext="{Binding Editor}" />

                <GridSplitter Grid.Column="3" ResizeDirection="Columns" />

                <!-- Preview pane (right) -->
                <DockPanel Grid.Column="4">
                    <!-- Serve status strip -->
                    <Border DockPanel.Dock="Bottom"
                            IsVisible="{Binding Preview.IsServing}"
                            Classes="preview-status-host"
                            Padding="4,2">
                        <TextBlock Text="{Binding Preview.ServeStatus}"
                                   Classes="muted"
                                   TextTrimming="CharacterEllipsis" />
                    </Border>

                    <!-- Markdown body preview -->
                    <md:MarkdownScrollViewer
                        IsVisible="{Binding Editor.HasDocument}"
                        Markdown="{Binding Editor.Body}" />

                    <!-- Placeholder when no document -->
                    <TextBlock Text="Select a file to preview"
                               IsVisible="{Binding !Editor.HasDocument}"
                               Classes="muted"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </DockPanel>
            </Grid>
        </Grid>

    </DockPanel>

</Window>
